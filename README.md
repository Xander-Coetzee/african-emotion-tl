# African Emotion TL  
**COS760 Group 16 Project**

## Setup Instructions

### Create a Virtual Environment

**Windows:**
python -m venv venv
venv\Scripts\activate
python -m pip install --upgrade pip
pip --version

**Mac:**
python3 -m venv venv
source venv/bin/activate
python3 -m pip install --upgrade pip
pip3 --version

**Virtual environment should be local**

### Dependencies

Install the following packages in your virtual environment:

**Windows:**
pip install datasets pandas

**Mac:**
pip3 install datasets pandas

## How to run the project:

**Windows:**
Python main.py

**Mac:**
Python3 main.py

### Additional
Hugging face link for BRIGHTER datasets: https://huggingface.co/datasets?search=brighter